# 隐私声明

本项目致力于保护用户健康数据的隐私与安全。请在使用前仔细阅读以下声明。

## 数据存储

- **本地存储**：所有健康数据（成员信息、指标记录、诊断用药、报告抽取结果）均存储在您本地部署的 PostgreSQL 数据库中，不会自动上传到任何云端服务。
- **数据控制权**：您对数据拥有完全控制权，可随时查看、修改、导出或删除。
- **软删除机制**：删除操作默认为软删除，数据在数据库中保留但不展示。如需彻底清除，请直接操作数据库。

## 模型 API 调用

- **多模态 API（必选）**：当您上传报告图片/PDF 进行结构化抽取时，文件内容会以 base64 编码发送给您配置的多模态模型 API（如 OpenAI GPT-4o、DeepSeek 等）。请查阅您所选 provider 的数据处理政策。
- **文字 API（可选）**：AI 咨询对话内容会发送给您配置的文字模型 API。
- **本地 LLM（可选）**：如配置 Ollama 等本地 LLM，对话内容完全在本地处理，不外发任何数据。

### 完全离线方案

如需完全离线运行（不向任何云端发送数据）：

1. 配置本地多模态模型（如 LLaVA via Ollama）作为多模态 provider
2. 配置本地 LLM 作为文字 provider
3. 注意：本地多模态模型在报告解析准确率上可能不如云端模型

## 数据安全建议

- **生产部署**：修改 `.env` 中的默认数据库密码
- **网络隔离**：将服务部署在内网，不暴露到公网
- **定期备份**：定期备份 PostgreSQL 数据卷
- **HTTPS**：如需远程访问，请配置反向代理 + HTTPS

## 第三方服务

| 服务 | 用途 | 数据发送 | 可选性 |
|------|------|----------|--------|
| 多模态模型 API | 报告图片/PDF 解析 | 图片/PDF 内容 | 必选（可配本地模型） |
| 文字模型 API | AI 咨询对话 | 对话文本 | 可选（可配本地 LLM） |
| 飞书开放平台 | 消息渠道接入 | 收发的消息内容 | 可选，V0.4 |

## 免责声明

本项目是健康数据管理工具，不提供医疗诊断服务。AI 生成的建议仅供参考，不能替代医生的专业判断。遇到急性症状或危急值，请立即就医。
